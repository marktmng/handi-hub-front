
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#576b49",
    },
    secondary: {
      main: "#8ba57d", // Optional accent
    },
  },
  typography: {
    fontFamily: "Roboto, sans-serif",
  },
});
